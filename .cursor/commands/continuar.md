# Continuar

Retomar trabalho interrompido na **branch da task** — sem repetir o que já está feito.

## Passos

1. Ler `.tasks/README.md` e `.tasks/<branch>.md` activa
2. `git branch --show-current` — se não for a branch da task, `git checkout <branch>`
3. `git status` + `git diff --stat`
4. Confirmar no código (não só no chat)
5. Próximo checkbox por fazer — ou wireframes UI se ainda em falta
6. Implementar só esse passo
7. `/verify` se tocaste Go/UI/Python

## Fim

Task pronta → `/integrar-main`

Português. Não commitar sem pedido.
