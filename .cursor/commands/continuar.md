# Continuar

Retomar trabalho interrompido sem repetir o que já está feito.

## Passos

1. Ler `.tasks/README.md` e task `.tasks/feat/*.md` activa
2. `git status` + `git diff --stat` — o que mudou localmente
3. Se existir transcript recente no chat, usar só para intenção — **confirmar no código**
4. Identificar o **próximo checkbox** por fazer na task
5. Implementar só esse passo (ou corrigir bug reportado)
6. `/verify` se tocaste Go/UI/Python

## Resposta

- 2–3 frases: onde paramos, o que falta, o que vais fazer agora
- Depois executar — não pedir ao utilizador para copiar comandos que podes correr tu

Português. Não commitar sem pedido.
